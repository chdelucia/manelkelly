export interface Boda {
    id: number;
    question: string;
    respuesta: string;
    img?: string;
    inicial: string;
    success: boolean
    error?: string;
    goldem?: boolean;
    goldemInicial?: string;
    
  }